version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leave-management-frontend
    ports:
      - "4200:80"
    environment:
      - API_URL=${API_URL}
    restart: always
